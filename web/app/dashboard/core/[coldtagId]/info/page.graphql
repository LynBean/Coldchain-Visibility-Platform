query CVP_DashboardCoreInfo_DisplayCoreColdtagById($coreId: ID!) {
  displayCoreColdtag {
    byId(coreId: $coreId) {
      id
      macAddress
      identifier
      events {
        basic {
          id
          connectionStatus
          eventTime
        }
      }
    }
  }
}
